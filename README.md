## RSA encoding/decoding implementation

### Command line usage:

./rsa -h

### Using as a library:

Keys_t k <br>
genkeys(&keys) <br>
encodeStream(keys, "filename") <br>
decodeStream(keys, "filename") <br>


